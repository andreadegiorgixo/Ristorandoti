/**
 * Mirror manuale di alcune costanti configurabili lato backend (vedi
 * {@code demo/.../config/DashboardProperties.java}, unico file di configurazione della Dashboard
 * aziendale). Se cambi un default lì, aggiorna anche qui: non c'è un endpoint che espone questi
 * valori al frontend.
 */

/** Lunghezza massima del testo della sezione Panoramica (DashboardProperties.overviewMaxLength). */
export const OVERVIEW_MAX_LENGTH = 4000;
