package com.ristorandoti.application.entity;

/** Stato di una candidatura a un'offerta di lavoro (versione minima del flusso). */
public enum StatoCandidatura {
    INVIATA,
    VISUALIZZATA,
    ACCETTATA,
    RIFIUTATA
}
